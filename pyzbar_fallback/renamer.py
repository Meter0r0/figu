import urllib.request
import os

print('Downloading fallback binary...')
# We will just write a pure python QR reader using a reliable 3rd party open source library like qrtools or write a simple script that uploads to a free API for decoding.
# Or better yet, we can use the 'boofcv' java command line tool.
